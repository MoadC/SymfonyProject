{{ form_start(form) }}
    <div class="col-md-12">
        {{ form_widget(form) }}
    </div>
    <div class="d-flex justify-content-center"> <button class="btn btn-info">{{ button_label|default('Save') }}</button></div>
    
{{ form_end(form) }}
